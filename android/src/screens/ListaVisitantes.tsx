import React from 'react'
import { Text, View } from 'react-native';
import { style } from '../theme/appTheme';

export const ListaVisitantes = () => {
  return (
    <View >
        <Text style={style.title}> Lista de visitantes</Text>
    </View>
  )
}